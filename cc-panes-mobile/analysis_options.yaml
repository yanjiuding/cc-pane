include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true

linter:
  rules:
    - prefer_final_locals
    - unawaited_futures
    - avoid_catches_without_on_clauses
